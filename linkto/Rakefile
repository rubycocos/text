require 'hoe'
require './lib/linkto/version.rb'

Hoe.spec 'linkto' do

  self.version = Linkto::VERSION

  self.summary = 'linkto - link_to helpers for google search, bing search, flickr photo search, flickr photo tag, etc.'
  self.description = summary

  self.urls    = ['https://github.com/rubylibs/linkto']

  self.author  = 'Gerald Bauer'
  self.email   = 'webslideshow@googlegroups.com'

  # switch extension to .markdown for gihub formatting
  self.readme_file  = 'README.md'
  self.history_file = 'HISTORY.md'

  self.extra_deps = [
    ['logutils' ]
  ]

  self.licenses = ['Public Domain']

  self.spec_extras = {
   :required_ruby_version => '>= 1.9.2'
  }


end
